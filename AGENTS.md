# Agent guide

This repository owns the Ralphy website, public library data, and web publishing
scripts. Use Bun, keep the checkout independent from sibling repositories, and
run `bun run build` before completing changes. Core CLI work belongs in
`alecs5am/ralphy`; public documentation belongs in `alecs5am/ralphy-docs`.
